{
  "description": "elm-es-client",
  "repository": "https://github.com/rubyisbeautiful/elm-es-client.git",
  "license": "MIT",
  "scripts": {
    "postinstall": "elm-package install -y",
    "watch": "elm-test --watch",
    "test": "elm-test"
  },
  "dependencies": {
    "elm": ">=0.19.0-bugfix2",
    "elm-test": ">=0.19.0-beta12"
  }
}
